{
  "name": "schema",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "node -e \"\"",
    "lint": "echo ok",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:update": "vitest run -u"
  },
  "dependencies": {
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}