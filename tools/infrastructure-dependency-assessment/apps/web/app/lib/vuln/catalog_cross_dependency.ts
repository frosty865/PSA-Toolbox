/**
 * Vulnerability Catalog - Cross-Dependency
 *
 * v1 minimal cross-dependency vulnerabilities. Uses the same trigger format as
 * catalog_energy (TriggerRule). Module-based OT/ICS vulnerabilities can be
 * added in future iterations.
 */

import type { VulnerabilityConfig } from '../report/vulnerability/vulnerability_types';

export const CROSS_DEPENDENCY_VULNERABILITIES: VulnerabilityConfig[] = [
  // Placeholder for future cross-dependency and OT/ICS module vulnerabilities
  // These will be expanded when module-based questions are integrated
];
