# Removing a tool: delete its folder, remove it from tools-manifest.json, run `pnpm sync:manifest` / build,
# and delete any line here that pointed at that tool (or `pnpm install` will fail).
packages:
  - "apps/*"
  - "packages/*"
  - "../cisa-site-assessment"
