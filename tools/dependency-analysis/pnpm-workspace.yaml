# Only this workspace (Next app + shared packages). Optional tools under tools/<id>/ are NOT workspace
# members — deleting a tool never breaks `pnpm install` here.
packages:
  - "apps/*"
  - "packages/*"
